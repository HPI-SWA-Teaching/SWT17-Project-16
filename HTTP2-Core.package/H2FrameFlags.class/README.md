A H2FrameFlags is a container class for flags from a H2Frame.

Instance Variables
	flagStorage:		<SmallInteger>

flagStorage
	- internal integer holding a bit for each flag representation. Should not exceed 1 Byte.
