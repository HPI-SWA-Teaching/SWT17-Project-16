A H2WebClient is a subclass of WebClient which support HTTP/2.

Instance Variables
	h2Session:		aModule to handle HTTP/2 communication.
	serverProtocolDictionary:		a dictionary to save servernames and their supported protocols. 

